package ee.spark.components.support
{
	import ee.spark.components.SkinnableItemRenderer;
	
	import flash.events.MouseEvent;

	/**
	 * Basic renderer for menu leafs. Is in essence just a SkinnableItemRenderer. This 
	 * class is mainly created for easy styling.
	 * 
	 * <p>By default the ee.spark.skins.MenuLeafItemSkin is set (using default.css).</p> 
	 */
	public class MenuLeafItem extends LeafItem
	{
		public function MenuLeafItem()
		{
		}
	}
}